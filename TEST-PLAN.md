# Test Plan — 동행 v1

Generated by /plan-eng-review on 2026-05-12
Branch: main
Repo: 동행 (Donghaeng)

> `/qa` · `/qa-only` 가 1차 input으로 읽음. 코딩 끝나면 이 plan 따라 검증.

---

## 화면 / 핵심 코드패스

```
앱 흐름
[+] Onboarding
  ├── 권한 요청 (Location, Microphone, Speech Recognition)
  ├── [GAP] [→E2E] Location permission denied → 화면 어떻게 되나?
  ├── [GAP] [→E2E] Microphone permission denied → TTS만 동작? 음성 입력만 끊김?
  └── 첫 진입 화면 (홈)

[+] 홈 (Shell B)
  ├── "오늘은 어떻게 보내고 싶으세요?" 헤딩 표시
  ├── 다음 여행 카드 (있을 때 / 없을 때)
  ├── 4 quick action 탭 핸들러
  ├── [GAP] [→E2E] "새로 짜기" 탭 → 간단 일정 시작 화면 이동
  ├── [GAP] [→E2E] "지금 떠나기" 탭 → 현장 컴패니언 즉시 진입
  └── 컴패니언 ambient 알림 (없을 때 / 있을 때)

[+] 간단 일정 시작 (NEW v1)
  ├── 3 input: 어디로 / 언제 / 어떤 결
  ├── [GAP] 도시 검색 (카카오 로컬 API)
  │   ├── [GAP] 한국 외 도시 입력 시 → 어떻게 처리?
  │   └── [GAP] 카카오 API rate limit (30k/일) 도달 시 → 사용자에게 무엇 보임?
  ├── [GAP] 날짜 input (DateTimePicker)
  ├── [GAP] 결 태그 3-4개 선택
  └── "시작" → LLM 호출 (system prompt에 user style + 도시 + 날짜) → 컴패니언 첫 제안 카드

[+] 현장 컴패니언 (★ NEW v1)
  ├── GPS 위치 받기 (expo-location)
  │   ├── [GAP] [→E2E] GPS 비활성 시 → 사용자 입력으로 fallback
  │   ├── [GAP] GPS 좌표 → 카카오 API 카테고리별 근처 검색 (CE7, FD6, AT4)
  │   └── [GAP] 인터넷 연결 끊김 → 캐시된 POI? 또는 오프라인 메시지?
  ├── LLM 호출: 위치 + user style + 카카오 응답 → 1-2개 카드 추천
  │   ├── [GAP] [→EVAL] LLM이 "여기 가보세요" 톤이 친구 같은지 (regression test)
  │   ├── [GAP] LLM 응답 시간 > 5초 시 → 로딩 표시? 타임아웃?
  │   └── [GAP] LLM API 실패 시 → 사용자에게 무엇 보임?
  ├── 음성 토글 (TTS 활성/비활성)
  └── 카드 like/dismiss → in-context 메모리에 저장

[+] TTS 대화 (Scenario 07)
  ├── 큰 잉크 마크 (96px) + listening ring pulse
  ├── 음성 입력 받기 (expo-speech-recognition)
  │   ├── [GAP] 한국어 음성 인식 정확도 (방언·노이즈)
  │   └── [GAP] 마이크 권한 거부 시 키보드 입력 fallback
  ├── LLM 호출 (스트리밍)
  ├── 네이버 클로바 TTS 응답 재생
  │   ├── [GAP] [→E2E] 백그라운드에서 재생 (화면 꺼짐)
  │   ├── [GAP] [→E2E] AirPods 더블탭 → 일시정지/재개
  │   ├── [GAP] 클로바 API 실패 시 fallback (디바이스 TTS?)
  │   └── [GAP] TTS 응답 시간 > 3초 시 사용자 경험
  └── 대화 종료 (X 버튼)

[+] 권한·인프라
  ├── Background mode entitlement (iOS)
  ├── Foreground Service (Android)
  ├── react-native-track-player 초기화
  └── [GAP] [→E2E] 앱 백그라운드 ↔ foreground 전환 시 TTS 끊김 없는지
```

---

## Affected Pages / 핵심 검증 포인트

| 화면 | URL/Route | 검증 |
|---|---|---|
| Onboarding | `/` 첫 진입 | 권한 3종(Location, Mic, Speech) 요청 흐름. 거부 시 graceful fallback |
| 홈 | `/(tabs)/home` | 헤딩 표시, quick action 4개 동작, ambient 알림 |
| 간단 일정 시작 | `/plan/new` | 카카오 도시 검색, 날짜·결 input, LLM 첫 호출 성공 |
| 현장 컴패니언 | `/(tabs)/companion` | GPS → 카카오 → LLM → 카드 표시. 음성 토글 |
| TTS 대화 | `/talk` | 음성 인식 → LLM → 클로바 TTS. 백그라운드 모드. 이어폰 컨트롤 |

---

## Key Interactions to Verify

- **첫 권한 흐름** — 셋 다 허용, 셋 다 거부, 일부만 거부 (Mic만 X 등)
- **간단 일정 시작 → 현장 컴패니언** — 일정 짠 직후 현장 모드로 자연스럽게 이어지는지
- **음성 토글 on/off** — 화면 켜진 상태에서 즉시 음성으로 전환
- **이어폰 컨트롤** — AirPods 탭/더블탭으로 일시정지·재개·다음
- **백그라운드** — 앱 백그라운드로 가도 TTS 끊김 없음. Lock screen 컨트롤 표시
- **언어 변경** — OS 언어가 한국어 외인 경우 (영어·일본어) 어떻게 동작?
- **음성 인식 노이즈** — 야외·교통·소음 환경에서 정확도

---

## Edge Cases

- **GPS 거부 / 비활성** — 사용자가 직접 도시·위치 입력 가능
- **인터넷 끊김** — "다시 연결되면 알려드릴게요" 같은 친구 톤 메시지
- **카카오 API rate limit** — 30k/일 도달 시 (v1엔 발생 가능성 낮음) graceful 처리
- **LLM API 응답 5초+** — 로딩 표시, 타임아웃 후 재시도 옵션
- **클로바 TTS 실패** — AVSpeechSynthesizer / Android TTS로 fallback (퀄 떨어지지만 동작)
- **카카오 응답 0개** — 새벽·외진 곳에서 근처 POI 없을 때 "지금은 한적하네요" 같은 응답
- **빈 state** — 처음 켰을 때 (다음 여행 없음, 컴패니언 알림 없음) 어색하지 않은지
- **긴 한국어 입력** — 47자 도시명·문의 (예: "OO시 OO구 OO동 OO 빌라 OO호") 끊김 없이
- **AirPods Pro 외 이어폰** — 갤럭시 버즈, 일반 유선 이어폰에서도 컨트롤 작동?

---

## Critical Paths (E2E 필수)

1. **첫 사용자 흐름** — 앱 설치 → onboarding → 권한 → 홈 → 일정 시작 → 현장 컴패니언 → TTS 사용. 끊김 없이 완료.
2. **재방문 흐름** — 앱 재실행 → 홈에서 이전 여행 정보 유지 → 현장 컴패니언이 *이전 컨텍스트* 인지 (in-context memory)
3. **이어폰 끼고 산책** — 화면 끄고 이어폰만으로 컴패니언과 5분 대화. 끊김·끄저짐 없음.
4. **권한 거부 후 재요청** — 처음 거부 → 설정에서 허용 → 앱 재실행 시 정상 작동
5. **컴패니언 신뢰 검증** — 추천 받은 카페·POI가 *실제로 존재*하고 영업 중인지 (카카오 데이터 정확도)
6. **★ 현장 컴패니언 view 전환** — A → ⊕ 탭 → C 모달 슬라이드업 → × 탭 → A 복귀. 데이터·스크롤 위치·음성 상태 보존되는지
7. **★ Listening state 직교** — A에서 음성 토글 ON → C 모달 진입 → C에서도 음성 계속 → C에서 음성 토글 OFF → A 복귀 시 OFF 상태 유지
8. **★ TTS 음성/일기 모드 전환** — 음성 모드에서 5번 대화 → 📜 탭 → 일기 모드에서 5번 대화가 시간순으로 표시 → 일기에서 텍스트 입력 → 응답 받음 → 🎙️ 탭 → 음성 모드 복귀 시 이전 대화 컨텍스트 유지
9. **★ ALERT 카드 (변수 감지)** — 비 예보 발생 → OpenWeather API 응답 → 컴패니언 voice가 변경 톤 (\"비 와요…\") + amber 배너 + 대안 카드 표시. 사용자가 가던 POI가 영업 종료면 즉시 alert → 대안
10. **★ RESCUE 카드 (사용자 trigger)** — 사용자가 음성 또는 텍스트로 \"여기 닫혔어\" / \"다른 곳\" 발화 → 컴패니언이 즉시 confirm + 대안 카드 1-2개. 응답 시간 < 3초
11. **카드 타입 연쇄** — NORMAL 보다가 → ALERT 발생 → 사용자 RESCUE 발화 → RESCUE 카드 → NORMAL 복귀. 한 세션에 다중 전환 매끄럽게
12. **★ TTS 음성 모드 중 변수 발생** — TTS 음성 대화 진행 중 OpenWeather에서 비 감지 → 컴패니언이 voice 톤 자연스럽게 전환 ("어, 비 와요…") + 잉크 마크 glow 미세 amber. 화면에 amber 배너 띄우지 *않음*. 사용자가 "보여줘" 발화 → 현장 컴패니언 화면 자동 navigate (ALERT 카드 표시). "괜찮아" 발화 → 음성 대화 계속, glow만 dim 유지
13. **★ TTS 일기 모드 중 변수 발생** — 일기 모드에서 ALERT 자동 entry 추가 ("동행: 비 와요…") + "[지도로 보기]" 작은 청자 링크. 링크 탭 → 현장 컴패니언 navigate. 사용자가 직접 텍스트로 답장하면 entry 추가하며 대화 계속
14. **★ TTS RESCUE 발화 처리** — 음성/일기 어느 모드든 "여기 닫혔어" "다른 곳" 같은 키워드 감지 → 컴패니언 즉시 "아 그러면…" 응답 + 대안 음성/텍스트 제시. 사용자가 "카드로" 추가 발화 시에만 현장 컴패니언으로 전환

---

## Eval (LLM 출력 품질)

`/eval` 또는 수동 검증 필요:

- **친구 톤 유지** — 응답이 "친근한 존댓말"인지. "그래서·" "음·" 같은 어색한 한국어 X
- **개인화 일관성** — system prompt에 user style 넘긴 후, 추천이 그 style과 정합하는지
- **돌발 변수 대응** — "비 온다고 했는데 다른 곳 알려줘"에 대해 사용자 과거 발언 인용하며 응답하는지
- **거짓 정보** — LLM이 존재하지 않는 카페·주소를 만들어내지 않는지 (RAG로 카카오 결과만 사용)

---

## 회귀 (Regression)

v1 단계엔 아직 회귀 없음 (코드 0줄). v2 이후 — 디자인 시스템 추가 화면 (편지 일정, POI 리스트 등)이 들어올 때 v1 핵심 흐름이 망가지지 않는지 회귀 테스트 필수.

---

## 단위 테스트 + LLM Eval (D7 결정, 2nd review 2026-05-12)

### Vitest — services/ 단위 테스트 (Day 1 셋업)

```
services/companion/
├── card-resolver.test.ts        # state machine 전이 8 케이스
│   • NORMAL → ALERT (weather trigger)
│   • NORMAL → ALERT (POI 영업종료)
│   • ALERT → RESCUE (user "닫혔어")
│   • RESCUE → NORMAL (user "고마워")
│   • ALERT → NORMAL (user "괜찮아")
│   • ALERT → NORMAL (5min timeout)
│   • AWAY 직교 (네트워크 끊김)
│   • AWAY 복귀
├── llm-orchestrator.test.ts     # ID 검증 로직
│   • happy path: recommended_ids 모두 카카오 응답에 있음
│   • hallucination: LLM이 모르는 id 반환 → fallback 트리거
│   • 빈 응답: 카카오 0개 → "지금은 한적하네요"
│   • timeout > 5s → 로딩 표시
└── state.test.ts                # session store actions

services/intent.test.ts          # D4 intent extraction
├── "지도로 보여줘" → show_map
├── "카드로" → show_card
├── "여기 닫혔어" → rescue
├── "그거 보여달라고 했잖아" → continue (false positive 방지)
├── "지도 보여줘 그리고 사진도" → show_map (multi-intent)
└── confidence 낮은 케이스

stores/user-style.test.ts        # ★ REGRESSION
├── persist hydration: AsyncStorage → store 복원
├── action: addLike(POI) → tags 업데이트
└── 앱 재시작 시뮬레이션 (mock AsyncStorage)
```

### promptfoo — LLM eval suite (Phase 5 시작 시)

```
evals/
├── friend-tone.eval.yaml
│   • input: "지금 어디 가볼까"
│   • assert: "친근한 존댓말" 톤. 어색한 한국어 X
│   • count: 10 cases, threshold 8/10 pass
├── hallucination.eval.yaml      # D2 검증
│   • input: Kakao 5 POI + 사용자 발언
│   • assert: recommended_ids 가 input id 안에 있음
│   • count: 20 cases (다양한 카테고리)
├── user-style-consistency.eval.yaml
│   • input: user-style { 자연: true, 도시: false } + 도시 카페 추천
│   • assert: 응답에 자연 요소 포함 또는 그 톤
│   • count: 15 cases
└── intent-extraction.eval.yaml  # D4 검증
    • input: ASR 텍스트 변형 30종
    • assert: 정확한 intent 매핑
    • count: 30 cases
```

### Phase별 테스트 진입점

| Phase | 추가되는 테스트 |
|---|---|
| Phase 1 (Day 1) | Vitest + promptfoo 셋업. 빈 테스트 1개 통과 |
| Phase 3 (Day 4-5) | services/poi.test.ts (Kakao client) |
| Phase 4a (Day 6-9) | card-resolver + llm-orchestrator + state 단위 테스트. hallucination eval |
| Phase 4b (Day 10-11) | 지도 모달 E2E (수동) |
| Phase 5a (Day 10-12) | intent.test.ts + intent-extraction.eval. friend-tone eval |
| Phase 5b (Day 13) | user-style.test.ts (persist regression) |
| Phase 6 | TEST-PLAN.md 전체 critical paths 수동 검증 |
