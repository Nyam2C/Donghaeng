# =============================================================================
# 동행 v1 · 환경 변수 예시
# 사용법: 이 파일을 `.env` 로 복사 후 값 채우기. .env 는 git ignore (절대 commit X)
# =============================================================================

# -----------------------------------------------------------------------------
# Kakao Local API (server)
# https://developers.kakao.com → 내 애플리케이션 → REST API 키
# 일 30k 요청 무료
# -----------------------------------------------------------------------------
KAKAO_REST_KEY=

# -----------------------------------------------------------------------------
# OpenWeather API (server, ALERT 트리거)
# https://openweathermap.org/api → My API Keys
# 무료: 60 calls/min, 1000 calls/day
# -----------------------------------------------------------------------------
OPENWEATHER_KEY=

# -----------------------------------------------------------------------------
# Frontend (Expo)
# 모바일 디바이스에서 docker host 접속 시 host IP. localhost 안 됨
# 시뮬레이터: http://localhost:3000
# 실기기: http://<호스트 IP>:3000
# -----------------------------------------------------------------------------
EXPO_PUBLIC_API=http://localhost:3000

# -----------------------------------------------------------------------------
# D13 Mock layer (Phase 2-5 frontend가 backend 없이 화면 확인할 때)
# true 시 services/__mocks__/ fixture 응답 사용
# -----------------------------------------------------------------------------
EXPO_PUBLIC_USE_MOCKS=false

# -----------------------------------------------------------------------------
# Claude Code CLI 인증
# 환경변수 X — 호스트 ~/.claude/ 를 server 컨테이너에 bind mount (docker-compose.yml)
# 호스트에서 `claude login` 1회 실행 필수
# -----------------------------------------------------------------------------
