/**
 * 동행 디자인 시스템 — Theme entry
 *
 * `docs/DESIGN.md` 의 모든 토큰을 코드로 표현.
 * 어느 화면도 토큰을 *벗어나지 말 것* (CLAUDE.md 규칙).
 *
 * Phase 1 Day 2a 에서 freeze. Day 3 Scaffold 부터 이걸 import 해서 사용
 */

export * as colors from './colors'
export * as fonts from './fonts'
export * as spacing from './spacing'

export { light as lightColors, dark as darkColors } from './colors'
